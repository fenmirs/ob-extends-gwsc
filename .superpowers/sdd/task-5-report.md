# Task 5: 注音弹窗 — 完成报告

## 实现内容
- 替换 `src/ruby-modal.ts` stub 为完整实现，包含 `RubyModal`（预览+拼音输入+取消/确认按钮，支持统一拼音和逐字拼音两种格式）和 `buildRubyHtml`（解析拼音并生成 `<ruby>` HTML）
- 在 `src/styles.css` 末尾添加 `.heti-ruby-modal` 样式

## 构建验证
- `npm run build` 成功，无报错

## 修改文件
- `src/ruby-modal.ts` — 完整实现替换 stub
- `src/styles.css` — 添加 2 行样式

## 问题
- 无
